name: Wake App Up

on:
  schedule:
    - cron: '0 */6 * * *'

jobs:
  wake-up:
    runs-on: ubuntu-latest
    steps:
      - name: Send request to app
        run: curl -s "${{ secrets.STREAMLIT_APP_URL }}" > /dev/null
